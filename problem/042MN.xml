<problem display_name="Number properties and operations" markdown="&gt;&gt;If the digit in the tens place of 37,241 is increased by one and the digit in the thousands place is decreased by one, how has the number been changed?&lt;&lt;&#10;= 990&#10;" weight="0.0">
<script type="text/python" system_path="python_lib">
def hintfn1(answer_ids, student_answers, new_cmap, old_cmap):
	global expect
	aid = answer_ids[0]
	ans = student_answers[aid]
	if new_cmap.is_correct(aid):
		hint='Your answer has been submitted'
		new_cmap.set_hint_and_mode(aid,hint,'always')
		new_cmap.set_property(aid,'npoints',1)
		new_cmap.set_property(aid,'correctness','corrrectnr')
	else:
		hint='Your answer has been submitted'
		new_cmap.set_hint_and_mode(aid,hint,'always')
		new_cmap.set_property(aid,'npoints',1)
		new_cmap.set_property(aid,'correctness','incorrectnr')
	old_cmap=new_cmap
</script>
  <p>If the digit in the tens place of 37,241 is increased by one and the digit in the thousands place is decreased by one, how has the number been changed?</p>
  <numericalresponse answer="990">
    <formulaequationinput label="If the digit in the tens place of 37,241 is increased by one and the digit in the thousands place is decreased by one, how has the number been changed?"/>
		<hintgroup hintfn="hintfn1"/> 
   </numericalresponse>
</problem>
